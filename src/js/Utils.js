// Get all the elements that match the selector
export const getInputs = (selector) => document.querySelectorAll(selector);

// Create a DOM element
export const createElement = (type) => document.createElement(type);